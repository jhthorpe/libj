README
	JHT, Febuary 7, 2022 : created

para is a subdirectory of LIBJ written in C++, which is aimed at implementing the features
that help run a hybrid OpenMP/OpenMPI programs.

OpenMP  : handles the parallelization within a node. 
OpenMPI : handles the parallelization between nodes

